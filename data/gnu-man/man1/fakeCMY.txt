CREATE(1)                                           User Commands                                          CREATE(1)



NAME
       Create - Create a fake CMY data file from a CMYK profile.

DESCRIPTION
       Create  a  fake  CMY  data file from a CMYK profile, Version 1.5.1 Author: Graeme W. Gill, licensed under the
       AGPL Version 3 usage: fakeCMY [option] profile.icm fake.ti3 Diagnostic: Unknown flag

       -v     verbose

       -r res set surface point resolution (default 3)

       -l tlimit
              set total ink limit, 0 - 400% (estimate by default)

       -L klimit
              set black ink limit, 0 - 100% (estimate by default)

       Author: Graeme W. Gill, licensed under the AGPL Version 3 usage: fakeCMY [option] profile.icm fake.ti3  Diag‐
       nostic: Unknown flag

       -v     verbose

       -r res set surface point resolution (default 3)

       -l tlimit
              set total ink limit, 0 - 400% (estimate by default)

       -L klimit
              set black ink limit, 0 - 100% (estimate by default)



Create a fake CMY data file from a CMYK profile, VersAugust52013                                           CREATE(1)
